name: .NET Framework Tests with ACT

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Restore NuGet packages
        run: nuget restore NEO_ACT_Plugin.sln

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.1

      - name: Download ACT zip
        run: |
          $actUrl = "https://advancedcombattracker.com/download.php?id=57"
          $zipPath = "$env:USERPROFILE\AdvancedCombatTracker.zip"
          Invoke-WebRequest -Uri $actUrl -OutFile $zipPath

      - name: Extract ACT zip
        run: |
          $destination = "$env:USERPROFILE\AdvancedCombatTracker"
          Expand-Archive -Path "$env:USERPROFILE\AdvancedCombatTracker.zip" -DestinationPath $destination -Force

      - name: Set up ACT reference
        run: |
          $actTarget = "C:\Program Files (x86)\Advanced Combat Tracker"
          if (!(Test-Path $actTarget)) {
            New-Item -ItemType Directory -Path $actTarget -Force | Out-Null
          }
          Copy-Item -Path "$env:USERPROFILE\AdvancedCombatTracker\Advanced Combat Tracker.exe" -Destination "$actTarget\Advanced Combat Tracker.exe" -Force

      - name: Build solution
        run: msbuild NEO_ACT_Plugin.sln /p:Configuration=Debug /p:ReferencePath="C:\Program Files (x86)\Advanced Combat Tracker"

      - name: Run tests
        shell: powershell
        run: |
          $vsPath = &"C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.TestTools -property installationPath
          if (-not $vsPath) {
              Write-Error "Visual Studio installation with test platform not found."
              exit 1
          }
          $vstest = Join-Path $vsPath "Common7\IDE\Extensions\TestPlatform\vstest.console.exe"
          if (-not (Test-Path $vstest)) {
              Write-Error "vstest.console.exe not found at $vstest"
              exit 1
          }
          Write-Host "Using vstest.console.exe at $vstest"
          & $vstest "**/bin/Debug/*Test*.dll"
